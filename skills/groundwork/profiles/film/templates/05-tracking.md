# {{goal}} — tracking

{{vocab.work_unit}}s and their shots, dependencies, critical path. Production status mirror (survives sessions).

> **This is a status mirror of the shot board, not the shot board itself.** Your shot board owns the authoritative render/approve state of every shot. This file rolls that up for production management — what's in the pipeline, what it cost, what's approved. When this file and the board disagree, **the board wins**; re-sync from it. In an Ikenga workspace the board is `com.ikenga.studio` and you re-sync via `mcp__studio__storyboard_list_cells` / `render_status`.
>
> Only the coordinator (producer) writes the status entries below. Sequence owners report Definition-of-Done back; the coordinator records the result.

Status legend: `[ ]` not started · `[~]` in progress · `[x]` done · `[!]` blocked
Shot status legend: `queued` · `generating` · `rendered` · `approved` · `reshoot`

<!-- groundwork:auto:start round-fold -->
<!-- last_action: review · {{date}} -->
_No review passes yet. After `groundwork review` runs, this block carries a one-paragraph summary of what the most recent Round changed in this doc + where._
<!-- groundwork:auto:end round-fold -->

---

## Studio project

- **Studio project:** _(id / path — the one project this film's shots live in)_
- **Last synced from Studio:** _(date — when the shot table below was reconciled against the board)_

## Shot status (mirror of the Studio board)

_One row per shot, grouped by sequence. This mirrors the Studio shot board for production visibility — it does **not** replace it. `Engine / track` is the render engine + parallel track; `Cost` is the actual render/compute spend rolling up to the Budget table in `01-plan.md`; `Approved` is the per-shot sign-off (`storyboard_set_approved` in Studio)._

| Shot | Sequence | Scene · reel | Studio cell | Shot status | Engine / track | Cost | Approved |
|---|---|---|---|---|---|---|---|
| _e.g._ 12A | S3 | 3 · R1 | cell_12a | rendered | remotion / GPU-soft | $0.04 | ☐ |
| _e.g._ 12B | S3 | 3 · R1 | cell_12b | approved | remotion / GPU-soft | $0.04 | ☑ |
| | | | | | | | |

---

## Critical path

_Hand-written. The 1-line sequence of locks + production milestones that must land in order. Update by hand when it changes; re-derive at the start of each phase._

```
logline + treatment  →  {{vocab.freeze_gate_noun}} (lookbook + anchors)  →  generate shots (Studio)  →  approve shots  →  compose deliverables
```

**Parallel tracks** that can start once the {{vocab.freeze_gate_noun}} holds:
- _e.g._ "Look/lookbook + anchor-consistency track — locks the visual language every shot inherits"
- _e.g._ "Music / voice / sound track — runs beside shot generation"
- _e.g._ "Deliverable-spec + captions track — prepares export beds while shots render"

---

## How to use this file

- Each section below is one {{vocab.work_unit}} — its goal, the shots it contains (in Studio), deps, DoD.
- The matrix + wave plan + critical path are generated by `groundwork orchestrate` from these sections.
- Add a {{vocab.work_unit}}: write a `## WP-NN — title` section; orchestrate picks it up on next run.
- Keep the shot table above in sync with Studio; don't hand-invent shot state here.

## {{vocab.work_unit}} matrix

<!-- groundwork:auto:start wp-matrix -->
<!-- last_action: init · {{date}} -->
_No {{vocab.work_unit}}s defined yet. Add sections below and run `groundwork orchestrate`._
<!-- groundwork:auto:end wp-matrix -->

## Wave plan

<!-- groundwork:auto:start wave-plan -->
<!-- last_action: init · {{date}} -->
_Generated by `groundwork orchestrate`. Waves map to the production schedule in `01-plan.md` — which sequences enter the Studio pipeline when, ordered by dependency + render-budget headroom._
<!-- groundwork:auto:end wave-plan -->

## Critical-path graph

<!-- groundwork:auto:start critical-path -->
<!-- last_action: init · {{date}} -->
_Generated by `groundwork orchestrate` from the dep edges. Distinct from the hand-written one-liner above — this is the computed DAG, that's the human summary. Both stay in sync via review passes._
<!-- groundwork:auto:end critical-path -->

---

## {{vocab.work_unit}}s, grouped by phase

> {{vocab.work_unit}}s are grouped under `## Phase N — title` headers. Use `(sketch)` on phase headers whose contents aren't fully detailed yet — current phase fully detailed, next phase sketched, later phases stubbed. The orchestrate action reads `## WP-NN` headings regardless of phase group.

## Phase 1 — _title_

### WP-01 — _replace with a real {{vocab.work_unit}}_

- **Goal:**
- **Owner:**
- **Scene + reel:**
- **Shots (Studio cells):** _(list the cell ids this sequence contains — Studio owns their state)_
- **Anchors / locations used:**
- **Depends on ({{vocab.freeze_gate_noun}} / other sequences):**
- **Render budget (est. cost):**
- **Sub-plan:** _(optional — link to NN-*.md if one exists)_
- **DoD (observable signal):** _e.g._ all shots approved in Studio + composited into the sequence cut
- **Mock (if the lookbook or an anchor hasn't locked yet):**

## Phase 2 — _title_ (sketch)

_Sketched. Sequence sections here carry scope + open questions but not shot-level detail. Detail when the phase becomes current._

## Phase 3 — _title_ (stub)

_Stub. One paragraph of intent. Convert to sketched sequences when approaching._
