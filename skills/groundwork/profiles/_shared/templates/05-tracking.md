# {{goal}} — tracking

{{vocab.work_unit}}s, dependencies, critical path. Source of truth for execution status (survives sessions).

> Only the orchestrator writes the status checkboxes below. {{vocab.work_unit}} owners report Definition-of-Done back; the orchestrator records the result here.

Status legend: `[ ]` not started · `[~]` in progress · `[x]` done · `[!]` blocked

<!-- groundwork:auto:start round-fold -->
<!-- last_action: review · {{date}} -->
_No review passes yet. After `groundwork review` runs, this block carries a one-paragraph summary of what the most recent Round changed in this doc + where (e.g. "Round 7 folded six P1 gaps in: aspect-ratio model (G1, §2/§5/§6/§7/§13), capability matrix (G2, §5/§6/§13)...")._
<!-- groundwork:auto:end round-fold -->

---

## Critical path

_Hand-written. The 1-line execution flow that captures the linear bottleneck through this plan, plus the parallel tracks that can run beside it. Update by hand when the critical path changes; re-derive at the start of each phase._

```
{{vocab.work_unit_1}}  →  {{vocab.work_unit_2}}  →  {{vocab.work_unit_3}}  →  …
```

**Parallel tracks** that can start once {{vocab.first_milestone}} lands:
- _e.g. "Skills/blocks track — bundled skills + N blocks"_
- _e.g. "External-research workstream — runs in parallel; main plan unblocked either way"_

---

## How to use this file

- Each section below is one {{vocab.work_unit}} — its goal, files/scope, deps, DoD.
- The matrix + wave plan + critical path are generated by `groundwork orchestrate` from these sections.
- Add a new {{vocab.work_unit}}: write a `## WP-NN — title` section; orchestrate picks it up on next run.

## {{vocab.work_unit}} matrix

<!-- groundwork:auto:start wp-matrix -->
<!-- last_action: init · {{date}} -->
_No {{vocab.work_unit}}s defined yet. Add sections below and run `groundwork orchestrate`._
<!-- groundwork:auto:end wp-matrix -->

## Wave plan

<!-- groundwork:auto:start wave-plan -->
<!-- last_action: init · {{date}} -->
_Generated by `groundwork orchestrate`._
<!-- groundwork:auto:end wave-plan -->

## Critical-path graph

<!-- groundwork:auto:start critical-path -->
<!-- last_action: init · {{date}} -->
_Generated by `groundwork orchestrate` from the dep edges. Distinct from the hand-written one-liner above — this is the computed DAG, that's the human summary. Both stay in sync via review passes._
<!-- groundwork:auto:end critical-path -->

---

## {{vocab.work_unit}}s, grouped by phase

> {{vocab.work_unit}}s are grouped under `## Phase N — title` headers. Use `(sketch)` annotation on phase headers whose contents aren't fully detailed yet — convention is: current phase = fully detailed, next phase = sketched (scope only), later phases = stubbed (one paragraph). The orchestrate action reads `## WP-NN` headings regardless of which phase group they're in; the grouping is for human comprehension.

## Phase 1 — _title_

_One `### WP-NN — title` section per {{vocab.work_unit}} in this phase. Hand-authored. The orchestrate action reads these to derive the matrix above._

### WP-01 — _replace with a real {{vocab.work_unit}}_

- **Goal:**
- **Owner / scope:**
- **Depends on:**
- **Files / deliverables:**
- **DoD:**

## Phase 2 — _title_ (sketch)

_Sketched. WP sections here carry scope + open questions but not file-level detail. Detail when the phase becomes current._

## Phase 3 — _title_ (stub)

_Stub. One paragraph of intent. Convert to sketched-WPs when approaching._
