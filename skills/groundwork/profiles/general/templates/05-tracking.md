# {{goal}} — tracking

Workstreams, dependencies, critical path. Source of truth for execution status (survives sessions).

> Only the coordinator writes the status entries below. Workstream owners report Definition-of-Done back; the coordinator records the result.

Status legend: `[ ]` not started · `[~]` in progress · `[x]` done · `[!]` blocked

<!-- groundwork:auto:start round-fold -->
<!-- last_action: review · {{date}} -->
_No review passes yet. After `groundwork review` runs, this block carries a one-paragraph summary of what the most recent Round changed in this doc + where._
<!-- groundwork:auto:end round-fold -->

---

## Critical path

_Hand-written. The 1-line sequence of workstreams that must lock in order. Update by hand when it changes._

```
{{first_workstream}}  →  {{second_workstream}}  →  {{third_workstream}}  →  …
```

**Parallel tracks** that can start once {{first_workstream}} locks:
- _e.g. "Stakeholder communications track"_
- _e.g. "Asset / content production track"_

---

## How to use this file

- Each section below is one workstream — its goal, owner, deliverables, deps, DoD.
- The matrix + wave plan + critical path are generated by `groundwork orchestrate` from these sections.
- Add a workstream: write a `## WP-NN — title` section; orchestrate picks it up on next run.

## Workstream matrix

<!-- groundwork:auto:start wp-matrix -->
<!-- last_action: init · {{date}} -->
_No workstreams defined yet. Add sections below and run `groundwork orchestrate`._
<!-- groundwork:auto:end wp-matrix -->

## Wave plan

<!-- groundwork:auto:start wave-plan -->
<!-- last_action: init · {{date}} -->
_Generated by `groundwork orchestrate`._
<!-- groundwork:auto:end wave-plan -->

## Critical-path graph

<!-- groundwork:auto:start critical-path -->
<!-- last_action: init · {{date}} -->
_Generated by `groundwork orchestrate` from the dep edges. Distinct from the hand-written one-liner above — this is the computed DAG, that's the human summary. Both stay in sync via review passes._
<!-- groundwork:auto:end critical-path -->

---

## Workstreams, grouped by phase

> Workstreams are grouped under `## Phase N — title` headers. Use `(sketch)` on phase headers whose contents aren't fully detailed yet.

## Phase 1 — _title_

### WP-01 — _replace with a real workstream_

- **Goal:**
- **Owner:**
- **Depends on (decision locks / other workstreams):**
- **Deliverables:**
- **Stakeholders to inform:**
- **Sub-plan:** _(optional — link to NN-*.md if one exists)_
- **DoD (observable signal):**
- **Mock (if a dependency hasn't locked yet):**

## Phase 2 — _title_ (sketch)

_Sketched._

## Phase 3 — _title_ (stub)

_Stub._
